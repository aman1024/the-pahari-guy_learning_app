<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thepahariguylearningjourney.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applog1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/applog1"
        android:supportsRtl="true"

        android:theme="@style/Theme.Thepahariguy_FirstApp">


        <activity android:name=".Fragment_btn"></activity>
        <activity android:name=".August20Activity" />
        <activity android:name=".May20Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>